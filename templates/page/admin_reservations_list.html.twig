{% extends 'base.html.twig' %}

{% block title %}Reservations list{% endblock %}

{% block content %}
    <div class="col-md-12">
        <h1>This page is "Reservations list"</h1>

        <table class="table-bordered table-hover table">
            <thead class="thead-dark">
            <tr>
                <th>ID</th>
                <th>Date from</th>
                <th>Date to</th>
                <th>Storage type</th>
                <th>Volume size</th>
                <th>User Id</th>
                <th>Has delivery</th>
                <th>Reservation details</th>
                <th>Edit reservation</th>
            </tr>
            </thead>
            <tbody>
            {% for reservation in reservations %}
                <tr>
                    <td>{{ reservation.id }}</td>
                    <td>{{ reservation.dateFrom|date('Y-m-d') }}</td>
                    <td>{{ reservation.dateTo|date('Y-m-d') }}</td>
                    <td>{{ reservation.storageTypeId }}</td>
                    <td>{{ reservation.storageVolumeId }}</td>
                    <td>{{ reservation.userId }}</td>
                    <td>
                        {% if reservation.hasDelivery==1 %}
                            yes
                        {% else %}
                            no
                        {% endif %}
                    </td>
                    <td><a href="#" class="text-dark btn btn-link admin-reservation-details" id="{{ reservation.id }}" data-toggle="modal" data-target="#reservationModal">Show details</a></td>
                    <td><a href="{{ path('admin_edit_reservation', {'id': reservation.id}) }}" class="text-dark btn btn-link admin-reservation-details">edit</a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="modal fade" id="reservationModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
            <div class="modal-dialog" role="document">
                <div class="modal-content">
                </div>
            </div>
        </div>
        <div class="modal fade" id="reservationEditModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
            <div class="modal-dialog" role="document">
                <div class="modal-content">
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('bundles/fosjsrouting/js/router.min.js') }}"></script>
    <script src="{{ path('fos_js_routing_js', { callback: 'fos.Router.setData' }) }}"></script>
    <script src="https://code.jquery.com/jquery-3.3.1.min.js"></script>
    {{ encore_entry_script_tags('index') }}
    {{ encore_entry_script_tags('chained') }}
{% endblock %}